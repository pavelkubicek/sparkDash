PORT=5555
# Listen address. Loopback by default (the dashboard exposes SSH + power controls);
# set to the host LAN IP or 0.0.0.0 to allow access from another machine.
BIND_HOST=127.0.0.1
LLM_PORT=8888
POLL_INTERVAL_GPU=2000
POLL_INTERVAL_CPU=2000
POLL_INTERVAL_NETWORK=2000
POLL_INTERVAL_STORAGE=5000
POLL_INTERVAL_LLM=2000
POLL_INTERVAL_BANDWIDTH=1000
# Optional: ssh -i path inside the container when the key is not a default OpenSSH name
# Model Launcher: allowlist base for card dirs, as they exist on the target Sparks
# MODEL_REPOS_BASE=/home/you/cluster/docker
# Hosts of integrated services when they do NOT run on the dashboard machine
# (unset = co-located, loopback). Browser-facing links (observer, engine web
# UI) use these too — they must resolve from the operator's browser.
# AI_PROXY_HOST=spark1.lan
# DEV_ENGINE_API_HOST=spark1.lan
# DEV_ENGINE_WEBUI_HOST=spark1.lan
# SSH_IDENTITY_FILE=/root/.ssh/id_ed25519